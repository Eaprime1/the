# AGENTS.md — Read This First

> For collaboration participants and tools entering this repository.
> Timestamp: `202605141806`

---

## What This Is

This is **the** — a prima terminal template built around the Cards of Deck
system: a lifecycle architecture for tracking a universe of content using
playing card metaphors.

You are entering an active working system. Things are in motion.

---

## The Deck System

The four origin suits are lifecycle states for everything in this project:

```
♦ Diamond  = Seed / being born / first recognition
♣ Club     = Working / active build / in motion
♥ Heart    = Aethereal / interactive / in relationship
♠ Spade    = Pinnacle / container of containers / stable anchor
```

Start with `DECK/DECK_SCHEMA.md`. Then read `DECK/CARD_INTAKE.md`.

---

## Active Participants

Multiple collaboration tools are working on this project simultaneously:

```
Claude      → branch: claude/setup-deck-system-XxYXG
ChatGPT     → branch: feature/cards-of-deck
Copilot     → .github/copilot-instructions.md
Gemini      → documents arriving from Google Drive
Perplexity  → research layer (not yet integrated)
NotebookLM  → archive synthesis (not yet integrated)
```

---

## Identity Icons

Each participant marks its own edits with one fixed icon, assigned here —
not invented mid-PR, not claimed as an existing rule by the agent itself.
Place the icon once, on the first PR/commit where that agent appears, as
an identity tag — not on every line or file touched afterward.

```
Claude      → (unassigned)
ChatGPT     → (unassigned)
Copilot     → (unassigned — 🍥 appeared self-applied in PR #24,
                not yet ratified; pending owner decision)
Gemini      → ♊ (assigned)
Perplexity  → (unassigned)
NotebookLM  → (unassigned)
```

Only the owner assigns icons here. If a tool suggests a "repository rule"
about icons that isn't listed in this table, it isn't a real rule — flag
it, don't apply it.

---

## Your First Steps

1. Read this file (done)
2. Read `NAVIGO.md` for navigation guidance
3. Read `DECK/DECK_SCHEMA.md` for the architecture
4. Check `turns/log.md` for the current session state
5. Check `DECK/components/liminal-readme.md` for what's open right now

---

## The Prime Rule

Do not force the archive to fit the first draft.

Let the system create enough shape that incoming material can reveal
where it already belongs.

---

## Ethics Check

Before any significant action, consult `tools/ethics_check.md`.

Do not:
- Force concepts into suits before they have resolved
- Build structure for content that hasn't arrived
- Abstract before you have three concrete examples
- Overwrite another agent's work without merging

---

## Branch Protocol

Develop on your designated branch.
Commit with clear messages.
Push when changes are complete.
Create a draft PR if one doesn't exist.
Never push to main directly.
